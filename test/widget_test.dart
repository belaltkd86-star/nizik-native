import 'package:flutter_test/flutter_test.dart';

import 'package:nizik_native/main.dart';

void main() {
  test('NizikApp can be constructed', () {
    const app = NizikApp();
    expect(app, isA<NizikApp>());
  });
}
